/**
 * Компонент панели режима редактирования сообщения.
 *
 * TODO: Реализовать верстку с текстом «Редактирование» и кнопкой отмены.
 * Примечание: в текущей реализации режим редактирования обрабатывается
 * непосредственно внутри MessageComposer через проп editingMessage.
 */
export default function EditModeBar() {
    return <div>Edit Mode Bar</div>
}
